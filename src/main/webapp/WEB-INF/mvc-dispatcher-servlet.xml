<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
        http://www.springframework.org/schema/jee
        http://www.springframework.org/schema/jee/spring-jee-2.5.xsd
        http://www.springframework.org/schema/cache
        http://www.springframework.org/schema/cache/spring-cache-3.2.xsd">

    <context:component-scan base-package="com.nearsoft.incubator" />

    <mvc:annotation-driven />
    <mvc:default-servlet-handler/>

    <cache:annotation-driven/>

    <!-- Configuration for view paths -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix">
            <value>/WEB-INF/pages/</value>
        </property>
        <property name="suffix">
            <value>.jsp</value>
        </property>
    </bean>

    <!-- Rest Template -->
    <bean id="restTemplate" class="org.springframework.web.client.RestTemplate" />

    <!-- Register JSON Converter for RESTful Web Service -->
    <bean class="org.springframework.web.servlet.mvc.annotation.AnnotationMethodHandlerAdapter">
        <property name="messageConverters">
            <list>
                <bean class="org.springframework.http.converter.json.MappingJacksonHttpMessageConverter" />
            </list>
        </property>
    </bean>

    <!-- HTTP JSON MessageConverter -->
    <bean id="jsonConverter" class="org.springframework.http.converter.json.MappingJacksonHttpMessageConverter">
        <property name="supportedMediaTypes" value="application/json" />
    </bean>

    <!-- Spring Cache w/ default JDK implementation -->
    <bean id="cacheManager" class="org.springframework.cache.support.SimpleCacheManager">
        <property name="caches">
            <set>
                <bean class="org.springframework.cache.concurrent.ConcurrentMapCacheFactoryBean" p:name="airports" />
                <bean class="org.springframework.cache.concurrent.ConcurrentMapCacheFactoryBean" p:name="airlines" />
            </set>
        </property>
    </bean>

    <!-- Airports Dao -->
    <bean id="jdbcAirportsDao" class="com.nearsoft.incubator.dao.AirportsDaoJdbcImpl">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- Airlines Dao -->
    <bean id="jdbcAirlinesDao" class="com.nearsoft.incubator.dao.AirlinesDaoJdbcImpl">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- Cache expiry for airports (in seconds) -->
    <bean id="airportsRepositoryImpl" class="com.nearsoft.incubator.repositories.AirportsRepositoryImpl">
        <property name="cacheExpiry" value="28800" />
    </bean>

    <!-- Cache expiry for airlines (in seconds) -->
    <bean id="airlinesRepositoryImpl" class="com.nearsoft.incubator.repositories.AirlinesRepositoryImpl">
        <property name="cacheExpiry" value="28800" />
    </bean>

    <!-- Inject API configuration -->
    <jee:jndi-lookup id="apiConfiguration" expected-type="com.nearsoft.incubator.util.FlightApiConfiguration" jndi-name="bean/configuration"/>

    <!-- Database parameters -->
    <jee:jndi-lookup id="dataSource" expected-type="javax.sql.DataSource" jndi-name="jdbc/flightfinder" />
</beans>